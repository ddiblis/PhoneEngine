
/*
public class Chapters {
    Chapters = {
        1: {
            1.000: {
                recievedTexts: List<string>,
                responses: {
                    string topShort: string;
                    string bottomShort: string;
                    string topResp: string;
                    string bottomResp: string;
                    float topSubChap: 1.001;
                    float bottomSubChap: 1.002;
                }
            }
        }
    }
}
*/

/*
    chapter, content, within content have flag, if flag, hold off, add content, then continue.
    add flag to beginning of string, check before each index, if index, run against regex, change
    into new value then push or find picture, etc. and push
    {{{gfstanding}}} = image
    [[[smileyface]]] = emoji
    (((honey))) = namechange oppertunity
*/

/*
    chapter, sub chapter, choice options, 1-2 texts, based on preferences set, insert name and dom or sub response.
    leads long term to easier story writing.
    no extra subchapters needed
*/

/*
    subchapters my implementation:
    longer time to build extra subchapters
    bigger file to query from.
    using dict, n=1
    can designate 1-300 normal responses
    301- 600 dom
    601-901 sub
*/